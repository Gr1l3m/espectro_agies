import numpy as np

espectro = np.zeros((2, 100))

espectro[0,10] = 1
#
# a = np.arange(0, 10+0.1, 0.1)
#
# for T in range(0, len(a)):
#     if T <= ts:
#         arreglo_tiempo[T] = scd
#     else:
#         arreglo_tiempo[T] = s1d /

# for T in range(0, 1, 0.1):
#     print(T)